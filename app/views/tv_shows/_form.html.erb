<%= form_with(model: tv_show, local: true) do |form| %>
  <% if tv_show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tv_show.errors.count, "error") %> prohibited this tv_show from being saved:</h2>

      <ul>
      <% tv_show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :show_time %>
    <%= form.text_field :show_time %>
  </div>

  <div class="field">
    <%= form.label :channel_id %>
    <%= form.collection_select :channel_id, Channel.all, :id, :name, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :is_favourite %>
  <%= form.check_box :is_favourite  %>
    <div class="field">
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
